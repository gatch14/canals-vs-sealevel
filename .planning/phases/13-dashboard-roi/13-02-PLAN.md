---
phase: 13-dashboard-roi
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/components/SidePanel.tsx
autonomous: true
requirements:
  - CIRC-01
  - CIRC-02
  - CIRC-03
  - CIRC-04
  - VIE-01
  - VIE-02
  - ROI-01
  - ROI-02
  - ROI-03
  - ROI-04

must_haves:
  truths:
    - "L'utilisateur voit EconomicPanel dans le panneau latéral à la position finale (après CandidatesPanel, avant ClearDataButton)"
    - "L'application compile et s'affiche sans régression des sections existantes"
  artifacts:
    - path: "src/components/SidePanel.tsx"
      provides: "Panneau latéral incluant EconomicPanel à la position finale"
      contains: "EconomicPanel"
  key_links:
    - from: "src/components/SidePanel.tsx"
      to: "src/components/EconomicPanel.tsx"
      via: "import nommé + JSX element"
      pattern: "import.*EconomicPanel.*from.*EconomicPanel"
---

<objective>
Intégrer `EconomicPanel` dans `SidePanel.tsx` — ajouter l'import et l'élément JSX à la position finale (après CandidatesPanel, avant ClearDataButton).

Purpose: Rendre le panneau Économie & ROI visible dans l'interface utilisateur.
Output: `src/components/SidePanel.tsx` modifié avec EconomicPanel en position 7 (dernière section accordéon).
</objective>

<execution_context>
@C:/Users/gatch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gatch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-dashboard-roi/13-CONTEXT.md
@.planning/phases/13-dashboard-roi/13-PATTERNS.md
@.planning/phases/13-dashboard-roi/13-01-SUMMARY.md

<interfaces>
<!-- État actuel de SidePanel.tsx — extrait du codebase -->

Structure actuelle de src/components/SidePanel.tsx (lignes 1-16 imports, lignes 99-128 corps scrollable) :

```typescript
// Imports existants (ne pas toucher) :
import { AlertCircle } from 'lucide-react'
import { ModeIndicator } from './ModeIndicator'
import { DrawingToolbar } from './DrawingToolbar'
import { CanalList } from './CanalList'
import { ElevationPanel } from './ElevationPanel'
import { CalculationPanel } from './CalculationPanel'
import { EcologyPanel } from './EcologyPanel'
import { DashboardPanel } from './DashboardPanel'
import { useElevation } from '../hooks/useElevation'
import { useRoutingWorker } from '../hooks/useRoutingWorker'
import { usePersistence } from '../hooks/usePersistence'
import { useCanalStore } from '../store/canalStore'
import { ClearDataButton } from './ClearDataButton'
import { CandidatesPanel } from './CandidatesPanel'

// Corps scrollable (lignes 99-128) :
<div className="flex-1 overflow-y-auto flex flex-col min-h-0">
  {/* Section 3 — Liste des canaux */}
  <div className="px-4 py-4 border-b border-white/[0.08]">
    <CanalList />
  </div>
  {/* Section 4 — Profil altimétrique */}
  <ElevationPanel />
  {/* Section 5 — Calcul d'impact */}
  <CalculationPanel />
  {/* Section 5b — Analyse écologique */}
  <EcologyPanel />
  {/* Section 6 — Dashboard Global */}
  <DashboardPanel />
  {/* Section 8 — Candidats mondiaux */}
  <CandidatesPanel />
  {/* Section 7 — Effacement données */}
  <div className="px-4 py-4 border-t border-white/[0.08] mt-auto">
    <ClearDataButton />
  </div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter EconomicPanel dans SidePanel.tsx</name>
  <files>src/components/SidePanel.tsx</files>

  <read_first>
    - src/components/SidePanel.tsx (lire en entier avant de modifier — voir l'état actuel des imports et la position exacte de CandidatesPanel et ClearDataButton)
  </read_first>

  <action>
Modifier `src/components/SidePanel.tsx` avec exactement 2 changements :

**Changement 1 — Ajouter l'import** après la ligne `import { CandidatesPanel } from './CandidatesPanel'` :
```typescript
import { EconomicPanel } from './EconomicPanel'
```

**Changement 2 — Ajouter l'élément JSX** entre `<CandidatesPanel />` et le div `ClearDataButton` :
```tsx
{/* Section 7 — Économie & ROI (accordéon) — Phase 13 */}
<EconomicPanel />
```

Résultat attendu dans le corps scrollable :
```tsx
<div className="flex-1 overflow-y-auto flex flex-col min-h-0">
  {/* Section 3 — Liste des canaux */}
  <div className="px-4 py-4 border-b border-white/[0.08]">
    <CanalList />
  </div>
  {/* Section 4 — Profil altimétrique (accordéon) */}
  <ElevationPanel />
  {/* Section 5 — Calcul d'impact (accordéon) — Phase 4 */}
  <CalculationPanel />
  {/* Section 5b — Analyse écologique (accordéon) — Phase 5 */}
  <EcologyPanel />
  {/* Section 6 — Dashboard Global (accordéon) — Phase 6 */}
  <DashboardPanel />
  {/* Section 8 — Candidats mondiaux pré-calculés (Phase 8) */}
  <CandidatesPanel />
  {/* Section 7 — Économie & ROI (accordéon) — Phase 13 */}
  <EconomicPanel />
  {/* Section 7 — Effacement données (Phase 7) */}
  <div className="px-4 py-4 border-t border-white/[0.08] mt-auto">
    <ClearDataButton />
  </div>
</div>
```

NE PAS modifier : les hooks (useElevation, useRoutingWorker, usePersistence), le bloc routingState, les sections 1-6, ni ClearDataButton.
  </action>

  <verify>
    <automated>cd /c/dev/gsd/science/canal && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import { EconomicPanel } from './EconomicPanel'" src/components/SidePanel.tsx` retourne `1`
    - `grep -c "EconomicPanel" src/components/SidePanel.tsx` retourne `2` (1 import + 1 JSX)
    - `grep -c "CandidatesPanel" src/components/SidePanel.tsx` retourne au moins `2` (import + JSX — non supprimé)
    - `grep -c "ClearDataButton" src/components/SidePanel.tsx` retourne au moins `2` (import + JSX — non supprimé)
    - `grep -c "DashboardPanel" src/components/SidePanel.tsx` retourne au moins `2` (import + JSX — non supprimé)
    - L'ordre dans le JSX : `DashboardPanel` avant `CandidatesPanel` avant `EconomicPanel` avant `ClearDataButton`
    - `npx tsc --noEmit` retourne exit code 0
    - `npm test -- --run 2>&1 | grep -E "passed|failed"` : 0 tests failed
  </acceptance_criteria>

  <done>
    SidePanel.tsx importe et rend EconomicPanel à la position finale, tous les tests existants restent GREEN, TypeScript compile sans erreur.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SidePanel → EconomicPanel | Import statique de composant — pas de données non sûres transmises |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | SidePanel.tsx modification | accept | Modification minimale (2 lignes). Risque de régression mitigé par la contrainte de ne toucher qu'à l'import et la position JSX. |
</threat_model>

<verification>
```bash
cd /c/dev/gsd/science/canal
npx tsc --noEmit
npm test -- --run 2>&1 | tail -10
```
Toutes les 223 suites existantes doivent rester GREEN. EconomicPanel est un composant sans tests unitaires propres (UI d'assemblage).
</verification>

<success_criteria>
- `src/components/SidePanel.tsx` contient `import { EconomicPanel } from './EconomicPanel'`
- `<EconomicPanel />` apparaît dans le JSX après `<CandidatesPanel />` et avant le div ClearDataButton
- `npx tsc --noEmit` retourne 0 erreur
- `npm test -- --run` : 0 tests en échec
- L'app démarre (`npm run dev`) sans erreur console
</success_criteria>

<output>
Après complétion, créer `.planning/phases/13-dashboard-roi/13-02-SUMMARY.md`
</output>
