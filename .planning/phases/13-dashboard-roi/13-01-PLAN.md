---
phase: 13-dashboard-roi
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EconomicPanel.tsx
autonomous: true
requirements:
  - CIRC-01
  - CIRC-02
  - CIRC-03
  - CIRC-04
  - VIE-01
  - VIE-02
  - ROI-01
  - ROI-02
  - ROI-03
  - ROI-04

must_haves:
  truths:
    - "L'utilisateur voit le break-even et le coût total (KPI) en tête du panneau pour le canal sélectionné"
    - "L'utilisateur voit les 6 co-produits (spiruline, aquaculture, minéraux, surface agricole, durée de vie, timeline habitabilité) avec leurs valeurs en intervalles"
    - "L'utilisateur voit les projections ROI à 25, 50 et 100 ans en M€ intervalles"
    - "L'utilisateur voit un tableau comparatif de tous ses canaux (sous-accordéon replié par défaut) trié par break-even croissant"
    - "Quand aucun canal n'est sélectionné, un message placeholder est affiché"
    - "Quand le profil altimétrique n'est pas chargé, une alerte amber est affichée"
    - "Quand le dessalement est désactivé, une bannière amber indique que les co-produits ne sont pas disponibles"
    - "Le panneau s'ouvre automatiquement quand un canal avec élévation est sélectionné"
  artifacts:
    - path: "src/components/EconomicPanel.tsx"
      provides: "Panneau accordéon Économie & ROI — 4 états, co-produits, ROI, tableau comparatif"
      exports: ["EconomicPanel"]
      min_lines: 200
  key_links:
    - from: "src/components/EconomicPanel.tsx"
      to: "src/hooks/useROI.ts"
      via: "useROI() hook call"
      pattern: "useROI\\(\\)"
    - from: "src/components/EconomicPanel.tsx"
      to: "src/hooks/useCircular.ts"
      via: "useCircular() hook call"
      pattern: "useCircular\\(\\)"
    - from: "src/components/EconomicPanel.tsx"
      to: "src/lib/roiEngine.ts"
      via: "calcAllCanalsRoi() useMemo"
      pattern: "calcAllCanalsRoi"
---

<objective>
Créer `src/components/EconomicPanel.tsx` — accordéon "Économie & ROI" affichant la chaîne de valeur complète d'un canal : indicateurs KPI (break-even + investissement), 6 co-produits, projections ROI@25/50/100 ans, et tableau comparatif multi-canaux.

Purpose: Exposer en UI toute la valeur économique calculée par useROI (Phase 12) et useCircular (Phase 11), couvrant les 10 requirements UI de la Phase 13.
Output: `src/components/EconomicPanel.tsx` avec 4 états exclusifs et structure accordéon conforme au design system des panels existants.
</objective>

<execution_context>
@C:/Users/gatch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gatch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dashboard-roi/13-CONTEXT.md
@.planning/phases/13-dashboard-roi/13-UI-SPEC.md
@.planning/phases/13-dashboard-roi/13-PATTERNS.md

<interfaces>
<!-- Types et contrats extraits du codebase. L'exécuteur doit les utiliser directement. -->

De src/types/roi.ts :
```typescript
interface RoiResult {
  totalAnnualValueMEur: Interval   // valeur totale/an en M€
  totalCostMEur: Interval          // coût construction + dessalement en M€  ← "Investissement total"
  breakEvenYears: Interval         // années avant rentabilité  ← [Infinity,Infinity] sans dessalement
  roi25: Interval                  // ROI net cumulé à 25 ans en M€
  roi50: Interval                  // ROI net cumulé à 50 ans en M€
  roi100: Interval                 // ROI net cumulé à 100 ans en M€
}
```

De src/types/circular.ts :
```typescript
interface CircularResult {
  spirulineValue: Interval     // €/an
  aquacultureValue: Interval   // €/an
  mineralsValue: Interval      // €/an
  arableLandKm2: Interval      // km²
  lifespanYears: Interval      // ans
  habitabilityYears: Interval  // ans
}
// circularResult === null quand desalinationEnabled === false
```

De src/types/calculation.ts :
```typescript
type Interval = [number, number]
```

De src/lib/roiEngine.ts (signature de calcAllCanalsRoi) :
```typescript
interface RoiSummary {
  canalId: string
  name: string
  breakEvenYears: Interval
  totalCostMEur: Interval
  totalAnnualValueMEur: Interval
}
function calcAllCanalsRoi(
  canals: Canal[],
  calcParams: CalcParams,
  desertFeatures: FeatureCollection
): RoiSummary[]
// Déjà trié par breakEvenYears[0] croissant, Infinity en dernier
```

De src/hooks/useROI.ts :
```typescript
export function useROI(): RoiResult | null
// Retourne null si : pas de canal sélectionné, < 2 points, pas de profil élévation
// Ne gate PAS sur desalinationEnabled — calcule ROI même sans dessalement
// breakEvenYears === [Infinity, Infinity] si totalAnnualValueMEur[0] <= 0
```

De src/hooks/useCircular.ts :
```typescript
export function useCircular(): CircularResult | null
// Retourne null si desalinationEnabled === false
```

De src/store/canalStore.ts (sélecteurs utilisés dans les panels) :
```typescript
useCanalStore((s) => s.selectedCanalId)   // string | null
useCanalStore((s) => s.canals)             // Canal[]
useCanalStore((s) => s.desalinationEnabled) // boolean
useCanalStore((s) => s.calcParams)         // CalcParams
// Canal.elevation : ElevationProfile | null (null = profil non chargé)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer EconomicPanel.tsx — accordéon complet avec 4 états</name>
  <files>src/components/EconomicPanel.tsx</files>

  <read_first>
    - src/components/DashboardPanel.tsx (pattern accordéon exact + helpers formatInterval/formatNumber)
    - src/components/EcologyPanel.tsx (pattern 4 états, dt/dd typography, separateurs internes, AlertCircle amber)
    - src/hooks/useROI.ts (vérifier la signature exacte du retour)
    - src/hooks/useCircular.ts (vérifier la signature exacte du retour)
    - src/lib/roiEngine.ts (vérifier la signature de calcAllCanalsRoi et RoiSummary)
    - src/data/desertZones.geojson (confirme que l'import existe pour calcAllCanalsRoi)
  </read_first>

  <action>
Créer `src/components/EconomicPanel.tsx` en suivant exactement les patterns documentés dans 13-PATTERNS.md. Instructions précises :

**Imports (dans cet ordre) :**
```typescript
import { useState, useEffect, useMemo } from 'react'
import { ChevronDown, AlertCircle, Leaf, Fish, Beaker, Wheat, Clock, Home, TrendingUp } from 'lucide-react'
import { useCanalStore } from '../store/canalStore'
import { useROI } from '../hooks/useROI'
import { useCircular } from '../hooks/useCircular'
import { calcAllCanalsRoi } from '../lib/roiEngine'
import desertZones from '../data/desertZones.geojson'
import type { FeatureCollection } from 'geojson'
import type { Interval } from '../types/calculation'
```

**Helpers de formatage (copier depuis DashboardPanel.tsx, NE PAS créer src/utils/format.ts — refactor hors scope) :**
```typescript
const DESERT_FEATURES = desertZones as unknown as FeatureCollection

function formatNumber(n: number, decimals: number = 3): string {
  if (n === 0) return '0'
  if (Math.abs(n) < 0.001) return n.toExponential(2)
  return n.toFixed(decimals)
}

function formatInterval(iv: Interval, unit: string, decimals: number = 3): string {
  return `[${formatNumber(iv[0], decimals)} – ${formatNumber(iv[1], decimals)}] ${unit}`
}

function formatBreakEven(iv: Interval): string {
  if (iv[0] === Infinity) return '—'
  return formatInterval(iv, 'ans', 0)
}
```

**Corps du composant :**

```typescript
export function EconomicPanel() {
  const selectedCanalId     = useCanalStore((s) => s.selectedCanalId)
  const canals              = useCanalStore((s) => s.canals)
  const desalinationEnabled = useCanalStore((s) => s.desalinationEnabled)
  const calcParams          = useCanalStore((s) => s.calcParams)

  const roiResult      = useROI()
  const circularResult = useCircular()

  const selectedCanal = canals.find((c) => c.id === selectedCanalId) ?? null

  const [isOpen, setIsOpen] = useState(true)
  useEffect(() => {
    if (selectedCanalId && selectedCanal?.elevation) setIsOpen(true)
  }, [selectedCanalId])

  const [isCompareOpen, setIsCompareOpen] = useState(false)

  const roiSummaries = useMemo(
    () => calcAllCanalsRoi(canals, calcParams, DESERT_FEATURES),
    [canals, calcParams],
  )

  // Dérivés booléens pour les 4 états exclusifs
  const noCanal   = !selectedCanalId
  const noProfile = selectedCanal !== null && !selectedCanal.elevation
  const noDesal   = selectedCanal !== null && !!selectedCanal.elevation && !desalinationEnabled
  const hasData   = selectedCanal !== null && !!selectedCanal.elevation && roiResult !== null
  // ...
}
```

**Structure JSX complète :**

Racine : `<div className="border-t border-white/[0.08]">` (séparateur identique à tous les panels)

Header accordéon : `<button onClick={() => setIsOpen(o => !o)} className="w-full h-8 px-4 flex items-center gap-2 text-left hover:bg-white/[0.04] transition-colors focus-visible:ring-2 focus-visible:ring-blue-400 focus-visible:ring-offset-2 focus-visible:ring-offset-gray-900 outline-none" aria-expanded={isOpen}>`
- ChevronDown size={14} `className={\`text-gray-400 transition-transform duration-200 ${isOpen ? 'rotate-180' : ''}\`}`
- span : `text-[12px] font-normal text-gray-400 uppercase tracking-wider` — texte : `Économie &amp; ROI`

Corps (visible si isOpen) :

**État A — aucun canal (`noCanal`) :**
```tsx
<div className="h-10 px-4 flex items-center">
  <p className="text-xs text-gray-500 italic text-center leading-relaxed w-full">
    Sélectionnez un canal pour voir l&apos;analyse économique
  </p>
</div>
```

**État B — sans profil (`!noCanal && noProfile`) :**
```tsx
<div className="h-10 px-4 flex items-center gap-1">
  <AlertCircle size={12} className="text-amber-400 shrink-0" />
  <p className="text-xs text-amber-400">
    Chargez le profil altimétrique pour l&apos;analyse économique
  </p>
</div>
```

**État C — dessalement désactivé (`noDesal && roiResult`) :** Afficher les KPI break-even et coût (seuls disponibles sans dessalement), puis bannière amber.
```tsx
<dl className="px-4 py-2 flex flex-col gap-1">
  <div className="flex flex-col gap-1">
    <dt className="text-[11px] text-gray-500 uppercase tracking-wider">Seuil de rentabilité</dt>
    <dd className="text-[13px] font-semibold text-white">{formatBreakEven(roiResult.breakEvenYears)}</dd>
  </div>
  <div className="flex flex-col gap-1">
    <dt className="text-[11px] text-gray-500 uppercase tracking-wider">Investissement total</dt>
    <dd className="text-[13px] font-semibold text-white">{formatInterval(roiResult.totalCostMEur, 'M€', 1)}</dd>
  </div>
</dl>
<div className="px-4 pb-3">
  <p className="text-[11px] text-amber-400 flex items-center gap-1">
    <AlertCircle size={12} className="shrink-0" />
    Activez les nœuds de dessalement pour les co-produits
  </p>
</div>
```

**État D — données complètes (`hasData`) :**

Section 1 — KPI (break-even + coût) :
```tsx
<dl className="px-4 py-2 flex flex-col gap-1">
  <div className="flex flex-col gap-1">
    <dt className="text-[11px] text-gray-500 uppercase tracking-wider">Seuil de rentabilité</dt>
    <dd className="text-[13px] font-semibold text-white">{formatBreakEven(roiResult.breakEvenYears)}</dd>
  </div>
  <div className="flex flex-col gap-1">
    <dt className="text-[11px] text-gray-500 uppercase tracking-wider">Investissement total</dt>
    <dd className="text-[13px] font-semibold text-white">{formatInterval(roiResult.totalCostMEur, 'M€', 1)}</dd>
  </div>
</dl>
```

Séparateur interne + Section 2 — Co-produits (visible si circularResult) :
```tsx
<div className="border-t border-white/[0.06] mt-2 pt-2 px-4 pb-3 flex flex-col gap-2">
  {circularResult && (
    <dl className="flex flex-col gap-1">
      {/* 6 lignes dt/dd avec icône */}
      <!-- Spiruline : Leaf size={12}, label "Spiruline", valeur formatInterval(circularResult.spirulineValue, '€/an', 0) -->
      <!-- Aquaculture : Fish size={12}, label "Aquaculture", valeur formatInterval(circularResult.aquacultureValue, '€/an', 0) -->
      <!-- Minéraux : Beaker size={12}, label "Engrais & minéraux", valeur formatInterval(circularResult.mineralsValue, '€/an', 0) -->
      <!-- Surface agricole : Wheat size={12}, label "Surface agricole créée", valeur formatInterval(circularResult.arableLandKm2, 'km²', 1) -->
      <!-- Durée de vie : Clock size={12}, label "Durée de vie estimée", valeur formatInterval(circularResult.lifespanYears, 'ans', 0) -->
      <!-- Habitabilité : Home size={12}, label "Timeline habitabilité", valeur formatInterval(circularResult.habitabilityYears, 'ans', 0) -->
    </dl>
  )}
</div>
```

Pour chaque ligne co-produit, la structure dt est :
```tsx
<div className="flex flex-col gap-1">
  <dt className="text-[11px] text-gray-500 uppercase tracking-wider flex items-center gap-1">
    <IconName size={12} className="shrink-0" />
    Label texte
  </dt>
  <dd className="text-[13px] font-semibold text-white">
    {formatInterval(circularResult.xxxValue, 'unité', decimales)}
  </dd>
</div>
```

Séparateur interne + Section 3 — Projections ROI (TrendingUp icon dans le header de section) :
```tsx
<div className="border-t border-white/[0.06] mt-2 pt-2 px-4 pb-3 flex flex-col gap-2">
  <p className="text-[11px] text-gray-500 uppercase tracking-wider flex items-center gap-1">
    <TrendingUp size={12} className="shrink-0" />
    Projections ROI
  </p>
  <dl className="flex flex-col gap-1">
    <div className="flex flex-col gap-1">
      <dt className="text-[11px] text-gray-500 uppercase tracking-wider">ROI à 25 ans</dt>
      <dd className="text-[13px] font-semibold text-white">{formatInterval(roiResult.roi25, 'M€', 1)}</dd>
    </div>
    <div className="flex flex-col gap-1">
      <dt className="text-[11px] text-gray-500 uppercase tracking-wider">ROI à 50 ans</dt>
      <dd className="text-[13px] font-semibold text-white">{formatInterval(roiResult.roi50, 'M€', 1)}</dd>
    </div>
    <div className="flex flex-col gap-1">
      <dt className="text-[11px] text-gray-500 uppercase tracking-wider">ROI à 100 ans</dt>
      <dd className="text-[13px] font-semibold text-white">{formatInterval(roiResult.roi100, 'M€', 1)}</dd>
    </div>
  </dl>
</div>
```

Séparateur interne + Section 4 — Sous-accordéon comparatif (visible si canals.length > 0) :
```tsx
<div className="border-t border-white/[0.06] mt-2 pt-2 px-4 pb-3 flex flex-col gap-2">
  {canals.length > 0 && (
    <>
      <button
        onClick={() => setIsCompareOpen((o) => !o)}
        className="flex items-center gap-1 text-left focus-visible:ring-2 focus-visible:ring-blue-400 focus-visible:ring-offset-2 focus-visible:ring-offset-gray-900 outline-none"
        aria-expanded={isCompareOpen}
      >
        <ChevronDown
          size={12}
          className={`text-gray-400 transition-transform duration-200 ${isCompareOpen ? 'rotate-180' : ''}`}
        />
        <span className="text-[11px] text-gray-400 uppercase tracking-wider">
          Comparer ({canals.length} canaux)
        </span>
      </button>
      {isCompareOpen && (
        <table className="w-full mt-1">
          <thead>
            <tr>
              <th scope="col" className="text-left text-[10px] text-gray-500 uppercase tracking-wider pb-1">Nom</th>
              <th scope="col" className="text-right text-[10px] text-gray-500 uppercase tracking-wider pb-1">Seuil</th>
              <th scope="col" className="text-right text-[10px] text-gray-500 uppercase tracking-wider pb-1">Coût</th>
              <th scope="col" className="text-right text-[10px] text-gray-500 uppercase tracking-wider pb-1">Valeur/an</th>
            </tr>
          </thead>
          <tbody>
            {roiSummaries.map((summary) => (
              <tr
                key={summary.canalId}
                className={summary.canalId === selectedCanalId ? 'bg-white/[0.04]' : ''}
                aria-current={summary.canalId === selectedCanalId ? 'true' : undefined}
              >
                <td className="text-left text-[11px] text-gray-300 py-[2px] truncate max-w-[96px]">
                  {summary.name}
                </td>
                <td className="text-right text-[11px] text-gray-300 py-[2px]">
                  {summary.breakEvenYears[0] === Infinity ? '—' : formatInterval(summary.breakEvenYears, 'ans', 0)}
                </td>
                <td className="text-right text-[11px] text-gray-300 py-[2px]">
                  {formatInterval(summary.totalCostMEur, 'M€', 1)}
                </td>
                <td className="text-right text-[11px] text-gray-300 py-[2px]">
                  {formatInterval(summary.totalAnnualValueMEur, 'M€', 1)}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
    </>
  )}
</div>
```

**Règles critiques à respecter :**
- `gap-1` (4px) pour les `gap` entre dt et dd — NE PAS copier le `gap-[2px]` des panels existants (UI-SPEC note)
- Em dash `–` (U+2013) dans tous les formatInterval — jamais le tiret simple `-`
- Break-even Infinity → `"—"` (tiret cadratin) — jamais `"Infinity"` ou `"∞"`
- Séparateurs internes : `border-white/[0.06]` — séparateur racine : `border-white/[0.08]`
- `text-[11px]` pour les labels, `text-[13px] font-semibold` pour les valeurs — jamais `text-sm`
- `aria-expanded` sur les deux boutons accordéon
- `aria-current="true"` sur la `<tr>` du canal sélectionné dans le tableau
- `scope="col"` sur tous les `<th>` du tableau
  </action>

  <verify>
    <automated>cd /c/dev/gsd/science/canal && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function EconomicPanel" src/components/EconomicPanel.tsx` retourne `1`
    - `grep -c "useROI" src/components/EconomicPanel.tsx` retourne au moins `1`
    - `grep -c "useCircular" src/components/EconomicPanel.tsx` retourne au moins `1`
    - `grep -c "calcAllCanalsRoi" src/components/EconomicPanel.tsx` retourne au moins `1`
    - `grep -c "formatBreakEven" src/components/EconomicPanel.tsx` retourne au moins `1`
    - `grep -c "Infinity" src/components/EconomicPanel.tsx` retourne au moins `1` (guard Infinity → "—")
    - `grep -c "aria-expanded" src/components/EconomicPanel.tsx` retourne `2` (accordéon principal + sous-accordéon)
    - `grep -c "aria-current" src/components/EconomicPanel.tsx` retourne au moins `1`
    - `grep -c "scope=\"col\"" src/components/EconomicPanel.tsx` retourne `4` (4 colonnes thead)
    - `grep -c "border-white/\[0.08\]" src/components/EconomicPanel.tsx` retourne `1` (séparateur racine uniquement)
    - `grep -c "border-white/\[0.06\]" src/components/EconomicPanel.tsx` retourne au moins `3` (séparateurs internes)
    - `grep -c "Économie" src/components/EconomicPanel.tsx` retourne au moins `1`
    - `grep -v "^[[:space:]]*//" src/components/EconomicPanel.tsx | grep -c "gap-\[2px\]"` retourne `0` (pas de gap-[2px] dans le code actif)
    - `npx tsc --noEmit` retourne exit code 0 (zéro erreurs TypeScript)
    - `grep -c "isCompareOpen" src/components/EconomicPanel.tsx` retourne au moins `2`
    - `grep -c "roiSummaries" src/components/EconomicPanel.tsx` retourne au moins `2`
  </acceptance_criteria>

  <done>
    EconomicPanel.tsx existe avec l'export nommé, consomme les 3 sources de données (useROI, useCircular, calcAllCanalsRoi), implémente les 4 états (A/B/C/D), affiche les 2 KPI + 6 co-produits + 3 projections ROI + tableau comparatif, et compile sans erreur TypeScript.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hooks → render | Les données de useROI et useCircular viennent du store Zustand — données utilisateur locales, pas de surface réseau dans ce plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | EconomicPanel — affichage intervalles | accept | Données 100% locales, pas de PII, pas de surface réseau. Panneau lecture seule. |
| T-13-02 | Denial of Service | calcAllCanalsRoi appelé dans useMemo | mitigate | useMemo avec dépendances [canals, calcParams] — pas de recalcul à chaque rendu. Si canals[] est très grand (>100), performance acceptable car pure function O(n). |
</threat_model>

<verification>
```bash
cd /c/dev/gsd/science/canal
npx tsc --noEmit
npm test -- --run 2>&1 | tail -20
```
Les tests existants (223 suite GREEN) ne doivent pas régresser — EconomicPanel.tsx est un nouveau fichier sans tests unitaires (pure UI d'assemblage de hooks déjà testés).
</verification>

<success_criteria>
- `src/components/EconomicPanel.tsx` existe et exporte `EconomicPanel`
- Le composant implémente les 4 états exclusifs (A/B/C/D)
- Tous les 10 requirements CIRC/VIE/ROI sont représentés dans l'UI (co-produits + ROI + durée de vie + habitabilité + tableau comparatif)
- Aucune valeur numérique affichée comme valeur ponctuelle — toutes en intervalles [min – max]
- Break-even Infinity affiché comme `"—"`
- `npx tsc --noEmit` retourne 0 erreur
- `npm test -- --run` maintient tous les tests existants GREEN
</success_criteria>

<output>
Après complétion, créer `.planning/phases/13-dashboard-roi/13-01-SUMMARY.md`
</output>
